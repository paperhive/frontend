module.exports = function (app) {
  app.controller('AnnotationCtrl', [function() {
    this.annotations = [];

    this.addAnnotation = function(annotation) {
      this.annotati
      return;
    };

    this.getAnnotations = function() {
      return this.annotations;
    };
  }]);
};
